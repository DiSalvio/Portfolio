<p id="notice"><%= notice %></p>

<div class="project_nav">
  <% if current_admin %>
    <%= link_to 'New Project', new_project_path, remote: true, id: 'new_project' %>
    <%= link_to 'Drafts', drafts_projects_path, id: 'project_drafts_link' %>
  <% end %>
</div>

<div id='project_form'>
</div>

<div id="projects">
  <%= render partial: 'project', collection: @projects, as: :project %>
</div>
